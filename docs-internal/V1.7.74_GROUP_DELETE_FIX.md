# v1.7.74 그룹 삭제·수정 안정성 개선 - 상세 기술 문서

> 작성일: 2026-03-16 | 대상: `src/soil/soil-script.js`
> 테스트 프로젝트에도 동일 수정 필요

---

## 목차

1. [문제 요약](#1-문제-요약)
2. [버그 1: 서브넘버 그룹 수정 시 번호 변경](#2-버그-1-서브넘버-그룹-수정-시-번호-변경)
3. [버그 2: 삭제 후 동일 접수번호 재입력 불편](#3-버그-2-삭제-후-동일-접수번호-재입력-불편)
4. [버그 3: 자동완성 리스트가 접수번호 입력란 가림](#4-버그-3-자동완성-리스트가-접수번호-입력란-가림)
5. [수정 파일 및 라인 참조](#5-수정-파일-및-라인-참조)
6. [테스트 프로젝트 적용 가이드](#6-테스트-프로젝트-적용-가이드)

---

## 1. 문제 요약

| # | 문제 | 심각도 | 영향 |
|---|------|--------|------|
| 1 | 서브넘버(321, 321-1) 그룹 수정 시 필지가 분리되어 접수번호가 321, 322, 323, 324로 변경됨 | **CRITICAL** | 데이터 무결성 |
| 2 | 삭제 후 동일 접수번호로 재입력하려면 개별 항목을 하나씩 삭제해야 함 | MAJOR | 사용성 |
| 3 | 작물 자동완성 리스트(position: fixed)가 접수번호 입력란을 가려서 클릭 불가 | MINOR | 사용성 |

---

## 2. 버그 1: 서브넘버 그룹 수정 시 번호 변경

### 재현 시나리오

1. 필지 2개, 각 필지에 작물 2개 등록 → 접수번호: 321, 321-1, 322, 322-1 (4건)
2. 목록에서 수정 버튼 클릭 → 그룹 수정 모드 진입
3. 수정 없이 저장 또는 일부 수정 후 저장
4. **결과**: 접수번호가 321, 322, 323, 324로 변경됨 (서브넘버 소실)

### 원인 분석

`populateFormForGroupEdit()` 함수에서 그룹의 각 레코드를 별도 필지 카드로 로드함.

**수정 전 코드** (기존):
```javascript
// 각 레코드를 1개의 필지 카드로 생성 → 4건이면 4개 필지 카드
groupLogs.forEach(log => {
    const parcel = log.parcels?.[0];
    if (parcel) {
        // ... 각각 별도 필지 카드 생성
        this.parcels.push(newParcel);
        this.renderParcelCard(newParcel, this.parcels.length);
    }
});
```

**문제**: 321(콩)과 321-1(고추)은 **같은 필지의 다른 작물**인데, 별도 필지 카드 2개로 로드됨.
저장 시 필지 4개 × 작물 1개 = 321, 322, 323, 324로 번호 재배정.

### 수정 내용

`populateFormForGroupEdit()` — `parcelIndex` 기준으로 그룹화하여 같은 필지의 작물을 합침.

**수정 후 코드** (~line 2233):
```javascript
// parcelIndex 기준으로 그룹화 (서브넘버 = 같은 필지의 다른 작물)
const parcelMap = new Map();
groupLogs.forEach(log => {
    const pIdx = log.parcelIndex || 1;
    if (!parcelMap.has(pIdx)) {
        parcelMap.set(pIdx, []);
    }
    parcelMap.get(pIdx).push(log);
});

// parcelIndex 순서대로 필지 카드 생성
const sortedParcelIndices = [...parcelMap.keys()].sort((a, b) => a - b);
sortedParcelIndices.forEach(pIdx => {
    const logsForParcel = parcelMap.get(pIdx);
    const firstLog = logsForParcel[0];
    const parcel = firstLog.parcels?.[0];
    if (!parcel) return;

    // 같은 필지의 여러 작물을 합침
    const mergedCrops = [];
    logsForParcel.forEach(log => {
        const logParcel = log.parcels?.[0];
        if (logParcel?.crops) {
            logParcel.crops.forEach(c => mergedCrops.push({ ...c }));
        }
    });

    const parcelId = `parcel-${this.parcelIdCounter++}`;
    const newParcel = {
        id: parcelId,
        lotAddress: parcel.lotAddress || '',
        isMountain: parcel.isMountain || false,
        subLots: parcel.subLots ? [...parcel.subLots] : [],
        crops: mergedCrops.length > 0 ? mergedCrops : [{ name: '', area: '' }],
        category: parcel.category || '',
        purpose: parcel.purpose || '',
        note: parcel.note || ''
    };
    this.parcels.push(newParcel);
    this.renderParcelCard(newParcel, this.parcels.length);
});
```

### 추가 수정: 접수번호 입력란 기본번호만 표시

**수정 전** (~line 2172):
```javascript
this.receptionNumberInput.value = firstLog.receptionNumber || '';
// → "321-1"이 들어갈 수 있음
```

**수정 후** (~line 2186):
```javascript
const baseRecNum = (firstLog.receptionNumber || '').split('-')[0];
this.receptionNumberInput.value = baseRecNum;
// → 항상 "321"만 표시
```

### 추가 수정: Firebase 삭제 로직 정확성

**수정 전** (~line 1761):
```javascript
const removedLogs = oldGroupLogs.slice(validParcels.length);
// → 필지 수 기준 슬라이스 (서브넘버 고려 안 함)
```

**수정 후** (~line 1770):
```javascript
const newIds = new Set(newLogs.map(l => l.id));
const removedLogs = oldGroupLogs.filter(l => !newIds.has(l.id));
// → 실제 새 레코드 ID와 비교하여 정확한 삭제
```

---

## 3. 버그 2: 삭제 후 동일 접수번호 재입력 불편

### 재현 시나리오

1. 321, 321-1, 322, 322-1 (4건) 데이터 존재
2. 321번 삭제 → 321-1은 남아있음
3. 새로 321번 입력 시도 → 중복 체크에서 "321 이미 존재" 오류
   - 중복 체크가 `log.receptionNumber.split('-')[0]` 으로 기본번호 비교
   - 321-1의 기본번호 = "321" → 중복으로 판정
4. 4건을 모두 삭제하려면 개별 삭제 4번 필요

### 수정 내용

#### 3-1. 그룹 삭제 기능 추가 (`deleteGroup` 메서드)

새 메서드 추가 (~line 500):
```javascript
async deleteGroup(groupId, baseReceptionNumber) {
    const groupLogs = this.sampleLogs.filter(log => log.groupId === groupId);
    const deleteIds = groupLogs.map(log => log.id);

    this.sampleLogs = this.sampleLogs.filter(log => log.groupId !== groupId);
    await this.saveLogs();
    this.filterAndRenderLogs();

    // Firebase에서도 삭제
    if (window.firestoreDb?.isEnabled()) {
        Promise.all(deleteIds.map(delId =>
            window.firestoreDb.delete('soil', parseInt(this.selectedYear), delId)
        ))...
    }

    // 삭제된 접수번호를 입력란에 세팅하여 재입력 편의 제공
    if (baseReceptionNumber && this.receptionNumberInput) {
        this.receptionNumberInput.value = baseReceptionNumber;
    }

    this.showToast(`${groupLogs.length}건이 삭제되었습니다. 접수번호 ${baseReceptionNumber}번으로 재입력할 수 있습니다.`, 'success');
}
```

#### 3-2. 삭제 버튼 클릭 핸들러 수정 (그룹 삭제 다이얼로그)

기존 단순 confirm → 그룹 여부에 따라 분기 (~line 3507):
```javascript
const deleteBtn = e.target.closest('.btn-delete');
if (deleteBtn) {
    const id = deleteBtn.dataset.id;
    const targetLog = this.sampleLogs.find(log => String(log.id) === String(id));

    if (targetLog?.groupId) {
        const groupLogs = this.sampleLogs.filter(log => log.groupId === targetLog.groupId);

        if (groupLogs.length > 1) {
            const baseNumber = (targetLog.receptionNumber || '').split('-')[0];
            const numbers = groupLogs.map(l => l.receptionNumber).join(', ');
            const choice = confirm(
                `같은 접수 그룹(${numbers})이 ${groupLogs.length}건 있습니다.\n` +
                `[확인] 그룹 전체 삭제 (삭제 후 ${baseNumber}번으로 재입력 가능)\n` +
                `[취소] 이 항목만 삭제`
            );

            if (choice) {
                this.deleteGroup(targetLog.groupId, baseNumber);
            } else {
                this.deleteSample(id, targetLog.receptionNumber);
            }
            return;
        }
    }

    if (confirm('정말 삭제하시겠습니까?')) {
        this.deleteSample(id, targetLog?.receptionNumber);
    }
}
```

**동작 흐름**:
- 그룹(2건 이상) → "[확인] 전체 삭제 / [취소] 이 항목만 삭제" 다이얼로그
- 단독 항목 → 기존 "정말 삭제하시겠습니까?" 다이얼로그

#### 3-3. 개별 삭제 시 접수번호 자동 세팅

`deleteSample(id, receptionNumber)` — 두 번째 인자 추가 (~line 470):
```javascript
async deleteSample(id, receptionNumber) {
    // ... 기존 삭제 로직 ...

    // 삭제된 접수번호의 기본번호를 입력란에 세팅 (재입력 편의)
    if (receptionNumber && this.receptionNumberInput) {
        const baseNumber = receptionNumber.split('-')[0];
        // 해당 기본번호가 더 이상 존재하지 않으면 입력란에 세팅
        const stillExists = this.sampleLogs.some(log =>
            (log.receptionNumber || '').split('-')[0] === baseNumber
        );
        if (!stillExists) {
            this.receptionNumberInput.value = baseNumber;
        }
    }
}
```

---

## 4. 버그 3: 자동완성 리스트가 접수번호 입력란 가림

### 재현 시나리오

1. 필지 카드에서 작물명 입력 → 자동완성 리스트 표시
2. 자동완성이 열린 상태에서 스크롤 또는 다른 곳 클릭
3. 자동완성 리스트가 `position: fixed; z-index: 100000`으로 화면에 남아있음
4. 접수번호 입력란 위치와 겹치면 클릭 불가

### 원인

- `crop-autocomplete-list`: `position: fixed; z-index: 100000` (style.css line 4215)
- `lot-address-autocomplete-list`: `position: absolute; z-index: 1000` (style.css line 4331)
- `blur` 이벤트에 200ms 딜레이 있지만, 뷰 전환이나 빠른 스크롤 시 정리되지 않음

### 수정 내용

#### 4-1. `closeAllAutocomplete()` 헬퍼 추가 (~line 297)

```javascript
closeAllAutocomplete() {
    document.querySelectorAll('.crop-autocomplete-list.show, .lot-address-autocomplete-list.show')
        .forEach(el => el.classList.remove('show'));
}
```

#### 4-2. `switchView()` 시 자동완성 닫기 (~line 274)

```javascript
switchView(viewName) {
    // 뷰 전환 시 열려있는 자동완성 리스트 모두 닫기
    this.closeAllAutocomplete();
    // ... 기존 로직 ...
}
```

#### 4-3. 폼 영역 스크롤 시 자동완성 닫기 (~line 3319)

`setupTypeSpecificEvents()`에 추가:
```javascript
// 폼 영역 스크롤 시 자동완성 닫기 (fixed 위치 리스트가 남아있는 것 방지)
const formView = document.getElementById('formView');
if (formView) {
    formView.addEventListener('scroll', () => this.closeAllAutocomplete(), true);
}
```

---

## 5. 수정 파일 및 라인 참조

모든 수정은 `src/soil/soil-script.js` 단일 파일.

| 수정 위치 | 내용 | 관련 버그 |
|-----------|------|-----------|
| ~line 274-303 | `switchView()` + `closeAllAutocomplete()` | 버그 3 |
| ~line 470-498 | `deleteSample(id, receptionNumber)` 인자 추가 + 접수번호 세팅 | 버그 2 |
| ~line 500-530 | `deleteGroup(groupId, baseReceptionNumber)` 새 메서드 | 버그 2 |
| ~line 1770-1778 | Firebase 삭제 로직 `newIds` 비교로 변경 | 버그 1 |
| ~line 2186 | 접수번호 `split('-')[0]` 기본번호만 표시 | 버그 1 |
| ~line 2233-2280 | `populateFormForGroupEdit()` parcelIndex 그룹화 | 버그 1 |
| ~line 3319-3323 | 폼 스크롤 시 `closeAllAutocomplete()` | 버그 3 |
| ~line 3507-3536 | 삭제 버튼 핸들러 그룹 삭제 분기 | 버그 2 |

---

## 6. 테스트 프로젝트 적용 가이드

테스트 프로젝트 (`sample-log-electron-test/project`)에도 동일 버그가 존재하므로 수정 필요.

### 주의사항 (sync-rule 참조)

1. **암호화 코드 보존**: 테스트 프로젝트의 `encryptionManager.init()` 호출, 암호화 스크립트 태그 유지
2. **STORAGE_KEY 접두사**: `test_soilSampleLogs` (test_ 접두사 유지)
3. **COLLECTION_PREFIX**: `test_` 유지

### 수정 방법

테스트 프로젝트의 `src/soil/soil-script.js`에 아래 8개 수정을 동일하게 적용:

#### 체크리스트

- [ ] `switchView()` — `this.closeAllAutocomplete()` 호출 추가 (함수 첫 줄)
- [ ] `closeAllAutocomplete()` — 새 메서드 추가 (`switchView` 바로 아래)
- [ ] `deleteSample(id)` → `deleteSample(id, receptionNumber)` — 인자 추가 + 접수번호 세팅 로직
- [ ] `deleteGroup()` — 새 메서드 추가 (`deleteSample` 바로 아래)
- [ ] `populateFormForGroupEdit()` — `groupLogs.forEach` → `parcelMap` 기반 그룹화로 교체
- [ ] `populateFormForGroupEdit()` — 접수번호 `split('-')[0]` 기본번호만 세팅
- [ ] 그룹 수정 저장 — Firebase 삭제 `oldGroupLogs.slice(validParcels.length)` → `newIds` 비교로 변경
- [ ] 삭제 버튼 핸들러 — 그룹 삭제 다이얼로그 분기 추가
- [ ] `setupTypeSpecificEvents()` — 폼 스크롤 시 `closeAllAutocomplete()` 리스너

### diff 참조 명령

```bash
# 메인 프로젝트에서 변경사항 확인
cd ~/sample-log-electron
git diff v1.7.73..v1.7.74 -- src/soil/soil-script.js
```

이 diff 출력을 참조하여 테스트 프로젝트에 동일 패치 적용.
